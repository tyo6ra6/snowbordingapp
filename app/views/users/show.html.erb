<div class="container mt-5">
  <div class="row">
    <div class="col-7 mx-auto">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">ニックネーム<%= current_user.nickname %></h5>
          <h6 class="card-subtitle mb-2 text-muted">登録メールアドレス<%= current_user.email %></h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.birth_day %>生まれ</h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.generation.name %></h6>
          <h6 class="card-subtitle mb-2 text-muted"><%= current_user.favorites_ski_resort %>スキー場</h6>
          <%= link_to( edit_user_path) do %>
            <button type="button" class="btn btn-outline-primary"><%='編集'%></button>
          <% end %>
        </div>    
      </div>
    </div> 
   </div>
 </div>